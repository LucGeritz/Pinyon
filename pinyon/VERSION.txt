0.1.2

pig:
- replaced split by explode
- added fullUrlNoParms

pinBaseController:
added pseudo event onBegin. 

pinWebApp
calls controller's onBegin. Only calls start() (or startXxx) if onBegin returns true.
It does process content though..
- adds 'fullurlnoparms' to settings

  